<header style="background-color:#cc2931;">
   <div class="container">
   <nav class="navbar navbar-light mb-2">
      <a class="navbar-brand" href="/">
         <img src="/assets/CHELLS-KITCHEN.png" alt="header-logo">
      </a>
      <% if user_signed_in? %>
            <%= link_to "購入", new_flavor_path, method: :get, class: "nav-link d-none d-md-inline" %>
            <%= link_to "マイページ", user_path(current_user), class: "nav-link d-none d-md-inline" %>
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "nav-link d-none d-md-inline" %>
         <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "nav-link d-none d-md-inline"  %>
         <% end %>
         <%= form_tag(search_flavors_path,:method => 'get', class: "nav-link d-none d-md-inline") do %>
           <%= text_field_tag :search %>
           <%= submit_tag 'Search', :name => nil %>
         <% end %>
   <div class="dropdown d-md-none" >
      <a class="btn btn-danger btn-sm dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          
      </a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdoenMenuLink">
         <% if user_signed_in? %>
            <%= link_to "購入", new_flavor_path, method: :get, class: "dropdown-item"%>
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "dropdown-item"%>
            <%= link_to "マイページ", user_path(current_user), class: "dropdown-item"%>
         <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "dropdown-item" %>
         <% end %>
         <%= form_tag(search_flavors_path,:method => 'get') do %>
           <%= text_field_tag :search %>
           <%= submit_tag 'Search', :name => nil %>
         <% end %>
      </div>
   </div>
   </nav>
</div>
</header>
